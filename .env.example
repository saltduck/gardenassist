# 复制此文件为 .env 并填入实际值，.env 已被 .gitignore 忽略，不会提交到 git

# OpenAI API Key（用于 Phase 5：养护建议、拍照识别、自动养护计划）
# 获取：https://platform.openai.com/api-keys
OPENAI_API_KEY=

# 本地用 wrangler 调试 Cloudflare Pages Functions 时，wrangler 读取 .dev.vars 而非 .env，
# 可将上述变量同步到 .dev.vars（.dev.vars 也已加入 .gitignore）。
# 部署到 Cloudflare Pages 时，在项目 Settings → Environment variables 中配置 OPENAI_API_KEY。
