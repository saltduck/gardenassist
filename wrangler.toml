# Cloudflare Pages + D1
# 部署前：在 Dashboard 创建 D1 数据库，将 database_id 填到下方；或直接在 Dashboard 绑定 D1 到 Pages 项目
name = "gardenassit"
pages_build_output_dir = "./dist"
compatibility_date = "2024-01-01"

[[d1_databases]]
binding = "DB"
database_name = "gardenassit-db"
database_id = "YOUR_D1_DATABASE_ID"
